=to JSON({
  GeneralParameters: tfsGeneralParameters.fileContent,
  KosParameters: tfsKOSParameters.fileContent,
  ServerParameters: to JSON({
    ServerName: string join(serverNames, ";"),
    ServiceName: string join(
      for s in serverNames
      return string join(serviceNames, ","),
      ";"
    )
  })
})