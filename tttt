=sort(
  distinct values(
    selectedKOSRows.RestartType
  ),
  function(x, y) x < y
)




=sort(
  distinct values(
    selectedKOSRows.Servername
  ),
  function(x, y) x < y
)





=sort(
  distinct values(
    selectedKOSRows.ServiceName
  ),
  function(x, y) x < y
)
