$processBody = @{
    processDefinitionId      = "RestartServiceProcess"
    processDefinitionVersion = -1
    variables = @{
        jiraIssueKey = "TEST-123"
    }
} | ConvertTo-Json -Depth 10




Invoke-RestMethod `
    -Method POST `
    -Uri "https://camunda-operate.seip-vip-prd-ocpgen10.qnb.com.tr/v2/process-instances" `
    -Headers @{
        Authorization = "Bearer $token"
    } `
    -ContentType "application/json" `
    -Body $processBody