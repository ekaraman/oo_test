=if list contains(restartType, "ALL")
then
  ["ALL"]
else
  concatenate(
    ["ALL"],
    sort(
      distinct values(
        KOSParameters[
          item.ortam = environment
        ].RestartType
      ),
      function(x, y) x < y
    )
  )