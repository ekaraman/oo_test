=if restartType = null
then []
else
  for r in restartType
  return split(r, "[|]")[-1]



=if serverName = null
then []
else
  for s in serverName
  return split(s, "[|]")[-1]


=if serviceName = null
then []
else
  for s in serviceName
  return split(s, "[|]")[-1]