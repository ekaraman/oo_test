= to_json({
  "DBServerName": "PSMPRDMGT01",
  "DbType": "MSSQL",
  "Username": "finops\\T99315509",
  "Password": ooFlowOutputs.Password,
  "Database": "Operasyon",
  "Command": string join([
    "EXEC [OPSADMIN].[sp_getParams] '",
    if serviceName = null then "" else serviceName,
    "', '",
    if serverName = null then "" else serverName,
    "', '",
    if restartType = null then "" else restartType,
    "', '",
    if environment = null then "" else environment,
    "'"
  ], ""),
  "Alfa": ""
})